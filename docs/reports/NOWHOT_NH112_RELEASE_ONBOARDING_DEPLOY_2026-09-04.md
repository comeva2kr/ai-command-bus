# NH112 업데이트 안내·튜토리얼 배포 보고

기준일: 2026-09-04 KST

대상: NH111 검수판을 보존한 Today/Live 대규모 업데이트

판정: **배포 전 GO, 운영 반영·공개 검증 대기**

## 이용자 결과

1. 처음 온 이용자는 Today와 Live를 어떻게 쓰는지 한 번만 본다.
2. 기존 이용자는 이번 대규모 업데이트 내용을 한 번만 본다.
3. Today에서 확인해도 Live에서 같은 창이 반복되지 않으며 반대 방향도 같다.
4. 공유 기사나 알림으로 바로 들어온 이용자의 상세 화면을 안내가 가리지 않는다.
5. 뒤로가기는 상세→안내→보던 목록 순서로 움직이며 목록에서 앱이 바로 닫히지 않는다.
6. Today 상세는 준비된 한국어 요약·사진·최초 시각·출처를 즉시 보여준다.

## 보존한 것

- 검수판 `SCE-0b991485de03a38a`, 콘텐츠 SHA
  `0b991485de03a38a3191fe43eedb27cf5773b0c7463065ad3f83455401238722`.
- 14개 분야 각 14건, 91개 두 분야 조합의 정확 합집합.
- 기사 제목·선택·순서·출처·사진·요약과 `filter_only`, 요청 경로 LLM 0.
- NH109의 중복 알림, 오래된 링크, 같은 창 원문 이동과 뒤로가기 수리.
- 유료벽 신규 수집 제외와 기존 공개 링크 보존.

## 최소 구현

- 기존 Live에 흩어져 있던 안내를 `public/notice-guide.js` 한 파일로 옮겨 Today와 공유했다.
- 기존 저장 키를 그대로 써 과거 이용자의 읽음 상태를 잃지 않는다.
- 새 방문 튜토리얼을 닫을 때 현재 릴리스도 함께 읽음 처리해 연속 팝업을 막는다.
- 공용 안내 파일만 SW v145에 추가했다. 새 의존성이나 별도 상태 서버는 만들지 않았다.
- Today의 기존 서버 정적 응답 경로에 광고/검색 메타를 주입하고 광고 렌더러는 하나로 유지했다.

## 배포 전 검증

| 검증 | 결과 |
| --- | --- |
| 전체 `node:test` | 1,925/1,925 PASS, 실패·SKIP 0 |
| 실제 Chrome 내비게이션 | 13/13 PASS |
| 격리 스테이징 | 102개 소스, 2,347건, 전 항목 PASS |
| Today 편집 결과 | 56건, 선택 4개 분야 각 14건, 후보 240건/출처 40곳 |
| 품질 게이트 | 검사 1,128·초안 298·최종 56·직접 복수 근거 오표기 0 |
| 요청 처리 | 홈 1ms, 상세 사전 준비, 요청 중 LLM 호출 0 |
| diff | `git diff --check` PASS |

## 운영 순서와 복귀

1. 포인터가 참조하는 11개 검수 판본을 VM 영속 볼륨에 복사하고 해시를 대조한다.
2. 판본 파일을 먼저 설치하고 `active.json` 포인터를 마지막에 원자 교체한다.
3. commit을 `main`에 push한 뒤 VM을 정확한 commit으로 reset하고 compose build한다.
4. `x-nowhot-check` 헤더로 Today/Live/API/정책 페이지/광고 파일을 공개 검증한다.
5. 결함 시 `NOWHOT_LOCAL_EDITORIAL=0`으로 기존 홈에 즉시 복귀한다.

광고 코드를 실을 수 있는 기술 조건과 Kakao AdFit·Google AdSense의 실제 심사 승인은
구분한다. 배포 뒤 공개 지면·정책 페이지·crawler 파일을 읽어야 신청 준비 완료로 판정한다.

## 작업 기록

- 입력 분류: 최종 지시 및 배포 승인.
- 작업 시작 전 확인한 MD / 자동 주입: 프로젝트 AGENTS.md, WRC 시작 게이트,
  memory summary, Ponytail 지시.
- 작업 시작 전 확인한 MD / 직접 읽음: WRC START_HERE·운영체계 정본·Wiki 규칙·
  enforcement·PMO board·report index, NowHot README·Blueprint·개발현황·NH111,
  Superpowers와 Orca 관련 지침.
- 미읽음/불가: 관련 없는 WRC 프로젝트 문서, 실제 광고 플랫폼 심사 내부 판정.
- 이번 작업 전용 파일: 공용 안내, 릴리스 소식, Today/Live 연결, 테스트, 본 보고서.
- 적용한 규칙: 목적·전체 경로 우선, 수용판 불변, 최소 공통 수정, 무료 검증,
  Codex·Claude·Cursor Grok 상호 반례 검수.
- First Principles 게이트: PASS.
- 개발현황 반영 / 대상 안정 ID: `NOWHOT-DEVELOPMENT-STATUS-001`.
- 개발현황 반영 / 변경 레코드: `DEVCHG-NOWHOT-20260904-197`.
- 개발현황 반영 / 대조 결과: 코드·고정판·실제 Chrome·전체 테스트·스테이징 일치.
- 금지선 준수: 유료 API/기사 우회·검수판 재편성 없음. 운영 반영 전 사실은 미완료로 표기.
- David 행동 필요 여부: 없음. 승인된 배포와 공개 검증을 이어서 수행한다.
- Telegram 알림 필요 여부: 배포 종료 시 기존 운영 규칙에 따라 1회.
- 이익 우선·과잉방어 점검: 기존 키와 브라우저 history를 재사용해 새 시스템을 만들지 않았다.
- 하지 않은 일: 판본 재생성, 전량 재요약, 유료 LLM 호출, 플랫폼 승인 완료 주장.
