# NOWHOT-B6-COUNTEREXAMPLE-REPAIR-001

## 판정

- 변경 ID: `DEVCHG-NOWHOT-20260811-055`
- 환경: 로컬 복제본
- 범위: 독립 적대검수에서 확인한 핵심 반례 4건만 최소 수리
- RED: 집중 실행 `189/194 PASS`, `5 FAIL`, 새 반례 `0/4`
- GREEN: 집중 실행 `194/194 PASS`, 전체 실행 `1107/1107 PASS`
- 현재 상태: 핵심 반례 `4/4` 회귀 통과, 독립 재검수 대기
- 운영 반영: 없음

과거 `NOWHOT-INDEPENDENT-CODEX-ADVERSARIAL-AUDIT-001`의 BLOCK 영수증은
수정하지 않았다. 이 문서는 그 검수에서 허용한 로컬 최소 수리의 후속 영수증이다.

## 수리 내용

| 반례 | 이전 동작 | 현재 동작 | 회귀 |
|---|---|---|---|
| `ADR-P0-01` | 주장 문장을 바꿔도 `lineage_pass` | 현재 주장 문장, 필드별 근거, 계약 지문을 재해시하고 불일치 시 HOLD | PASS |
| `ADR-P0-02` | verifier 없는 cache가 `verified_edit` | verifier 전체 기록과 계약·프롬프트·두 모델 버전이 맞는 cache만 사용 | PASS |
| `ADR-P1-04` | 변화 원자료가 없어도 대기 문장으로 PASS | `changedSincePrevious`가 없으면 `reader_copy_hold` | PASS |
| `ADR-P1-05` | `signal=0`이 양수 평가로 저장 | 저장 평가 삭제와 직전 명시 평가 특징 기여 역연산 | PASS |

계약 버전은 주장 계보 `v2/fingerprint v3`, LLM 런타임 `v2/prompt v2`,
독자 문장 `v6`으로 올렸다. 구형 또는 불완전한 캐시는 자동 승인하지 않는다.

## 남은 HOLD

1. 결정론적 사건 프레임이 만든 중요성·다음 확인 문장의 주장 단위 근거 지지
2. 다중 분야 이슈를 분야별 고유 사건으로 계산하는 충족 계약
3. 독자 문안 HOLD와 분야 부족을 마지막 검증판 서빙 게이트에 연결하는 경로
4. 실제 사람 2인 검수와 같은 독립 렌즈 재검수

따라서 이 수리는 사람 편집 품질, 장기 개인화 효용, 운영 승격, 실제 모델 품질을
증명하지 않는다. 다음 단계는 같은 독립 렌즈의 재검수지만 이번 지시 범위에서는
실행하지 않는다.
