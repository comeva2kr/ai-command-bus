# NOWHOT-B6-SERVEABLE-EDITION-GATE-001

## 판정

- 변경 ID: `DEVCHG-NOWHOT-20260811-057`
- 요구사항: `NH63-SERVEABLE-EDITION-GATE`
- 계약: `NOWHOT-EDITORIAL-SERVING-CONTRACT-001` v1
- 상태: 정확 응답 fail-closed 서빙 P0 로컬 수리 PASS, 후속 P1·운영 승격 BLOCK
- 환경: 로컬 복제본만

## RED

14개 단일 카테고리의 모닝·런치·이브닝 42개 응답을 직접 호출했다.

| 관측 | 결과 |
|---|---:|
| HTTP 200 | 42/42 |
| `human_annotation_ready` | 3/42 |
| `reader_copy_hold` | 38/42 |
| 기계 또는 분야 게이트 HOLD | 1/42 |

기존 `/api/today`는 실제 반환 응답의 reader 패킷과 분야 충족을 마지막 권한으로 사용하지 않았다. 따라서 품질 미달 판도 `publishable:true`와 HTTP 200으로 나갔다.

## 수리 계약

1. 독자에게 반환할 정확한 응답에서 정본 기계 검사, 독자 행 검사, 판 전체 문장 다양성, 선택 분야 충족을 다시 계산한다.
2. 네 검사를 모두 통과한 응답만 HTTP 200으로 제공하고 `packetId`, `editionId`, 분야 조합, 계약 버전의 불변 검증 영수증을 저장한다.
3. 현재판이 실패하면 같은 분야 조합의 24시간 이내 저장판만 후보로 삼는다. 현재 계약으로 다시 계산한 `packetId`가 저장 영수증과 같을 때만 검증판으로 제공한다.
4. 현재판과 검증판이 모두 실패하면 품질 미달 본문을 보내지 않고 HTTP 409 `EDITORIAL_EDITION_NOT_SERVEABLE`로 닫는다.
5. 자동 하루 세 판 제공 게이트와 표본 사람 검수·운영 배포 승인은 별도 증거로 유지한다.

우선순위는 `현재 정확 응답 PASS > 같은 분야 24시간 이내 검증 응답 PASS > HTTP 409`다.

## GREEN

- 집중 회귀: 17/17 PASS
- 현재 정확 응답과 검수 packetId 일치
- reader 또는 분야 HOLD 응답: HTTP 409
- 같은 분야 24시간 이내 검증판 폴백: PASS
- 저장 영수증 packetId 변조 또는 불일치: 폴백 거부, HTTP 409
- 전체 회귀: 1,114/1,114 PASS

## 독자 화면

- 검증판이 있으면 요청한 슬롯은 유지해 표시하고, 어느 이전 슬롯의 검증판을 대신 제공하는지 밝힌다.
- 검증판이 없으면 `새 브리핑을 검수 중입니다`와 `아직 보여드릴 검증판이 없습니다`를 표시하고 재시도를 제공한다.
- HOLD 판의 기사 본문은 화면에 노출하지 않는다.

## 남은 HOLD

- paragraph와 reader 7필드의 계보 지문 결박, 중요성 주장 단위 근거
- 사건 연속성 오탐·미탐과 관련기사 프레임 오염
- 다중 분야 이슈의 분야별 고유 사건 충족
- 중간 신호 뒤 평가 삭제의 정확 원복과 실제 개인화 효용
- 사람 편집 검수, 다일 안정성, 비용, 장애 복구 증거

다음 단계는 `B6-READER-LINEAGE-GROUNDING-REPAIR`다. 이 문서는 기사 사실성, 사람 편집 품질 PASS, 장기 개인화 효용, 운영 배포 가능을 증명하지 않는다. 운영 `nowhot.kr`, 광고·계정, GitHub push, 외부 게시에는 아무 변화가 없다.
