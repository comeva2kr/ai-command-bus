# NOWHOT-B6-EVENT-CONTINUITY-FRAME-REPAIR-001

## 판정

- 변경 ID: `DEVCHG-NOWHOT-20260811-059`
- 상태: `local_repair_pass_with_limits`
- 범위: 최근 브리핑 간 사건 정체성과 주 사건/관련기사 근거 역할
- 운영 반영: 없음
- 외부 LLM 호출: 0회
- 다음 단계: `B6-CATEGORY-UNIQUE-FULFILLMENT-REPAIR`

이번 단계는 확인된 사건 연속성 오판과 관련기사 프레임 오염을 로컬 코드와 회귀 테스트에서 폐쇄했다. 모든 사건의 의미 매칭 정확도, 기사 사실성, 사람 편집 품질, 운영 배포 가능성을 증명한 것은 아니다.

## RED 직접 재현

수리 전 집중 회귀는 `22/25 PASS · 3 FAIL`이었다.

1. `삼성 갤럭시 Z 폴드8 공개`와 `삼성 갤럭시 워치8 공개`가 삼성·갤럭시·공개라는 일반어 때문에 같은 사건으로 합쳐졌다. 반복 필터 뒤 현재 이슈가 지면에서 사라졌다.
2. `related_observation` 근거가 주 사건의 제목·리드·중요성·다음 확인 주장에 함께 결박됐다.
3. 관련기사의 `신약 임상시험 3상` 키워드가 무관한 문화행사의 중요성·다음 확인 프레임을 바꿔도 독자 문장 게이트가 통과했다.

## 수리 계약

### 사건 연속성 v4

- `공개`, `발표` 같은 일반어를 제거한 `dedupe.titleConcepts`를 사용한다.
- 공유 개념 3개 이상, 짧은 쪽 대비 0.5 이상일 때만 의미 연속성 후보로 인정한다.
- 공유 참조 ID, 정규 제목, 의미 개념 등 모든 사건 정체성 경로에서 `related_observation` 제목과 `itemId`를 제외한다.
- 같은 호르무즈 협상 사건은 기사와 클러스터 ID가 달라도 이어가되, 호르무즈라는 한 단어만 같은 별개 사건은 합치지 않는다.

### 근거 계보 v4 / fingerprint v5

- 주 사건의 `headline`, `whatHappened`, `paragraph`, `whyImportant`, `watchNext`는 lead·corroborating 근거만 결박한다.
- 관련기사는 교차보도·확산 관측인 `whyHot` 근거에만 보존할 수 있다.
- 관련기사 근거가 주 사건 claim에 붙으면 verifier가 명시적으로 HOLD한다.

### 독자 프레임 v8 / fingerprint v2

- 사건 프레임 입력은 `subject`, `headline`, `whatHappened`, `paragraph`로 제한한다.
- 관련기사 제목은 주 사건의 출처명·리드 제목·중요성·다음 확인 프레임을 바꾸지 못한다.
- 프레임 계약 ID·버전, 주 사건 근거 ID, 입력 해시를 독자 계보와 검수 패킷에 포함한다.
- 품질 계약은 v9, 검수 패킷은 v5로 올려 구형 판정을 자동 재사용하지 않는다.

## GREEN 증거

- 핵심 반례: `26/26 PASS`
- 관련 통합 회귀: `70/70 PASS`
- 전체 회귀: `1,121/1,121 PASS`
- 호르무즈 동일 사건 연속: PASS
- 폴드8/워치8 별개 사건 분리: PASS
- 관련기사의 사건 정체성·프레임 입력 제외: PASS
- 주 사건 claim의 관련기사 근거 격리: PASS

## 남은 HOLD

- 다중 `categoryIds` 이슈 한 건이 여러 선택 분야의 최소 깊이를 동시에 채우는 문제
- 중간 신호 뒤 평가 삭제의 정확한 개인화 원복
- 실제 사람 편집 검수, 다일 안정성, 장애 복구, 비용, 조합 확장성
- 운영 main 반영과 배포

다음 단계는 새 소스나 발행량 확대가 아니다. `B6-CATEGORY-UNIQUE-FULFILLMENT-REPAIR`에서 선택 분야마다 서로 다른 고유 사건 최소 깊이를 증명한다.
