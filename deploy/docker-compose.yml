# taste-feed 국내 서버 이전 — app + Caddy(자동 HTTPS) + 영속 볼륨.
# 사용: DOMAIN=your.domain docker compose up -d   (또는 .env에 DOMAIN 지정)
services:
  app:
    build: ..
    restart: unless-stopped
    environment:
      # 컨테이너 기본은 UTC다. 우리 소스는 대부분 한국 사이트라 "16:09" 처럼
      # 날짜 없는 시:분만 주는 게시판이 많고, 파서는 그걸 "오늘 그 시각"으로
      # 찍는다. 서버가 UTC면 그 값이 실제보다 9시간 앞서 미래가 되고,
      # isSaneDate가 통째로 버린다 — 2026-08-02 검수에서 heraldbiz 11건 전부
      # publishedAt이 null이었던 것과 정합한다(어댑터 주석도 "서버 TZ가 KST여야
      # 정확"이라고 스스로 적어 두었다).
      - TZ=Asia/Seoul
      - PORT=4000
      - FEED_DB=/data/feed.json        # 영속화 (재시작에도 유저 데이터 유지)
      - FEED_LIVE=1
      - FEED_REFRESH_MS=900000         # 15분마다 재수집
      - FEED_TRANSLATE=1               # 해외 소스 한글 번역
      # 새 오늘판 출시 플래그. 장애 시 0으로 바꾸고 컨테이너를 재시작하면 기존 홈으로 즉시 복귀.
      - NOWHOT_LOCAL_EDITORIAL=${NOWHOT_LOCAL_EDITORIAL:-1}
      # 검수 완료된 슬롯판만 읽는다. 포인터와 판본은 유저 데이터와 같은 영속 볼륨에 둔다.
      - NOWHOT_SLOT_CANONICAL_EDITION=${NOWHOT_SLOT_CANONICAL_EDITION:-1}
      - NOWHOT_SLOT_CANONICAL_POINTER=${NOWHOT_SLOT_CANONICAL_POINTER:-/data/slot-editions/active.json}
      # 오늘판의 카테고리 입력만 v2 분류 스냅샷으로 교체. v1이면 기존 분류 그대로.
      - NOWHOT_CATEGORY_ROUTING=${NOWHOT_CATEGORY_ROUTING:-v2}
      - PUSH_DIGEST_MS=3600000         # 시간당 관심글 digest push
      - VAPID_PUBLIC_KEY=${VAPID_PUBLIC_KEY}
      - VAPID_PRIVATE_KEY=${VAPID_PRIVATE_KEY}
      - VAPID_SUBJECT=${VAPID_SUBJECT:-mailto:comeva2kr@gmail.com}
      - ADMIN_TOKEN=${ADMIN_TOKEN}
      # IndexNow — 새 콘텐츠를 검색엔진에 우리가 먼저 알린다(네이버 2023-07-25
      # 지원 공지, 빙도 같은 엔드포인트). 브리핑이 하루 3회 갱신되는 구조에서
      # 크롤러를 기다리면 그만큼 색인이 늦고 곧 유입 손실이다.
      # 키는 우리가 정하는 임의 문자열이고, /{키}.txt 응답이 소유 증명이 된다.
      # 비어 있으면 통보 자체를 안 한다(no-op) — 키 없는 배포도 정상 동작.
      - INDEXNOW_KEY=${INDEXNOW_KEY:-}
      - PUBLIC_HOST=${PUBLIC_HOST:-nowhot.kr}
      # 브리핑 해설 생성 (src/feed/llm.js). 비어 있으면 호출 자체를 안 하고
      # 규칙 기반 브리핑이 그대로 나간다 — 키 없는 배포도 정상 동작한다.
      # LLM_MODEL 미지정 시 claude-sonnet-5 (2026-08-04 3모델 실측 비교 결과).
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - LLM_MODEL=${LLM_MODEL:-}
      # 최종 Today 이슈만 공개 원문을 메모리에서 읽고 검증된 한국어 장문 요약만 저장한다.
      # 실모델 카나리 통과 뒤에만 1로 연다. 0이면 기존 200자 발췌 상세가 즉시 롤백 경로다.
      - NOWHOT_ARTICLE_SUMMARY=${NOWHOT_ARTICLE_SUMMARY:-0}
      - NOWHOT_ARTICLE_SUMMARY_MODEL=${NOWHOT_ARTICLE_SUMMARY_MODEL:-claude-sonnet-5}
      - NOWHOT_ARTICLE_SUMMARY_VERIFIER_MODEL=${NOWHOT_ARTICLE_SUMMARY_VERIFIER_MODEL:-claude-sonnet-5}
      # 소셜 로그인 — 키가 설정된 provider만 앱에 버튼이 노출된다(auth.js 게이트).
      # 비어 있으면 그 provider는 비활성일 뿐, 앱은 익명으로 정상 동작한다.
      - PUBLIC_BASE_URL=${PUBLIC_BASE_URL:-}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID:-}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET:-}
      - KAKAO_CLIENT_ID=${KAKAO_CLIENT_ID:-}
      - KAKAO_CLIENT_SECRET=${KAKAO_CLIENT_SECRET:-}
      # 카카오톡 앱 원탭 로그인용 공개 JavaScript 키(시크릿 아님). 없으면
      # 카카오 로그인은 기존 리다이렉트 방식으로 그대로 동작한다.
      - KAKAO_JS_KEY=${KAKAO_JS_KEY:-}
      # 애드센스 게시자 ID(공개값). 설정 시 편집 페이지 로더 + 소유확인 메타 + /ads.txt 활성.
      - ADSENSE_CLIENT=${ADSENSE_CLIENT:-}
      # GA4 측정 ID ("G-…"). 설정 시 gtag 자동 주입 (미설정 = 미추적).
      - GA_MEASUREMENT_ID=${GA_MEASUREMENT_ID:-}
      # 카카오 애드핏 모바일 배너 광고단위(공개값).
      - ADFIT_UNIT_MOBILE=${ADFIT_UNIT_MOBILE:-}
      # 1이면 AdFit 심사 모드: /·/briefing·발행 아카이브·/report 같은 안정적인
      # 자체 편집 지면에만 한 단위를 두고, AdSense·쿠팡과 /live 광고는 끈다.
      # 기본값은 꺼짐. 재심사 직전 검증된 릴리스에서만 1로 전환한다.
      - ADFIT_ENABLED=${ADFIT_ENABLED:-0}
      # 쿠팡파트너스 실연동 — 3종 모두 있어야 상품 카드 활성(coupang.js).
      - COUPANG_PARTNER_ID=${COUPANG_PARTNER_ID:-}
      - COUPANG_ACCESS_KEY=${COUPANG_ACCESS_KEY:-}
      - COUPANG_SECRET_KEY=${COUPANG_SECRET_KEY:-}
      - COUPANG_SUB_ID=${COUPANG_SUB_ID:-nowhot}
      - NAVER_CLIENT_ID=${NAVER_CLIENT_ID:-}
      - NAVER_CLIENT_SECRET=${NAVER_CLIENT_SECRET:-}
    volumes:
      - feed-data:/data
    expose:
      - "4000"

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN}
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - app

volumes:
  feed-data:
  caddy-data:
  caddy-config:
